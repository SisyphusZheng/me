/**
 * FreshPress 主入口点
 * 提供单命令安装与初始化功能
 */

// 导入 CLI 工具
import "./cli.ts";

// 这个文件作为 https://freshpress.deno.dev 的入口点
// 会自动重定向到 CLI 工具
// 用户可以使用 deno run -A -r https://freshpress.deno.dev 命令来使用 FreshPress
